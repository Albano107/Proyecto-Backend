meta {
  name: Cambiar estado usuario (desactivar)
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/usuarios/{{id_usuario}}
  body: json
  auth: none
}

body:json {
  {
    "activo": false
  }
}

assert {
  res.status: eq 200
}
